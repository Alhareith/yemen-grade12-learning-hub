# المعمارية التقنية — دليل الثالث الثانوي اليمني

## القرار

سيبدأ المشروع كـ **تطبيق ويب ثابت سريع (Static Web App)** مبني بـ **React 19 + TypeScript + Vite + Tailwind CSS 4**. هذا الاختيار مناسب لأن وظيفة النسخة الأولى هي تصفح فهرس منظم من المصادر والانتقال إلى روابط خارجية موثقة؛ ولا تحتاج إلى تسجيل دخول أو قاعدة بيانات كي تكون نافعة للطالب من اليوم الأول.

## طبقات التطبيق

| الطبقة | التقنية | الدور |
|---|---|---|
| الواجهة | React 19 وTypeScript | عرض المسارات والمواد والبطاقات والتفاعلات بدقة وأمان نوعي |
| التنقل | Wouter | مسارات خفيفة لصفحة البداية وصفحة المادة وصفحة المصدر |
| التصميم | Tailwind CSS 4 وshadcn/ui وLucide | واجهة عربية متجاوبة، عناصر قابلة للوصول، وأيقونات واضحة |
| البيانات | ملفات JSON ساكنة داخل المشروع | فهرس المصادر، علامات الثقة، نوع المصدر، الوحدات، وتاريخ آخر تحقق |
| البحث والفلترة | منطق عميل بسيط | فلترة محلية فورية بلا طلبات شبكة أو انتظار |
| النشر والأداء | Vite static build + caching | تحميل سريع، حجم صغير، وإتاحة جيدة للهواتف ذات الشبكات المحدودة |

## تجربة الهاتف أولًا

يُصمم المحتوى بعرض أساسي بين 360 و430 بكسل، مع أهداف لمس لا تقل عن 48 بكسل، وخط عربي مريح، وقائمة تنقل سفلية قصيرة. لا تعتمد الصفحة على فيديو تلقائي أو صور كبيرة أو استدعاءات بيانات ضرورية عند الفتح. يحتفظ التطبيق فقط بمادة الطالب الأخيرة ومرشحه الأخير محليًا، دون بيانات شخصية أو حساب.

## نموذج البيانات

كل مصدر سيكون كائنًا يضم: `subject`، و`unit`، و`title`، و`resourceType`، و`platform`، و`url`، و`trustLevel`، و`linkStatus`، و`notes`، و`checkedAt`. هذا النموذج يتيح إظهار أزرار واضحة مثل «الكتاب الرسمي» و«شرح موصى به» و«تدرب الآن»، ويمنع خلط الموارد غير المراجعة بالمصادر الرسمية.

## قابلية التوسع

عند الحاجة إلى لوحة تحرير للمصادر، أو استقبال بلاغات الطلاب عن الروابط المتعطلة، أو حسابات المشرفين، يمكن ترقية المشروع إلى طبقة بيانات ومستخدمين. لكن لا ينبغي تحميل الطالب تعقيدًا غير ضروري في النسخة الأولى؛ يبقى الموقع العام سريعًا حتى بعد إضافة واجهة إدارة مستقلة لاحقًا.

## حدود النسخة الأولى

الموقع سيفهرس ويربط بالمصادر الخارجية ولا يعيد استضافة الكتب أو الفيديوهات. تظهر لكل مصدر درجة ثقة وتاريخ مراجعة، وتُمنع روابط الملفات التنفيذية والإعلانات من الواجهة العامة. سيُضاف فحص دوري للروابط في مرحلة إدارية لاحقة، لا في جهاز الطالب.
