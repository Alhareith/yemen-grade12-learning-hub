# المعمارية التقنية — دليل الثالث الثانوي اليمني

## 1. حالة المنتج الحالية

المشروع هو **تطبيق ويب تعليمي ثابت ومحلي أولًا (Static, local-first learning app)** لطلاب الثالث الثانوي في اليمن، مبني بـ **React 19 + TypeScript + Vite + Tailwind CSS 4**.

لم يعد المنتج مجرد فهرس روابط. المسارات الأساسية الحالية هي:

1. **التعلّم بالبرومبتات:** اختيار حالة الطالب والمادة والوحدة ثم توليد برومبت تعليمي مضبوط للمستوى والمنهج.
2. **المحاكاة:** تشغيل محاكاة التفاضل والتكامل، حفظ الجلسة محليًا، التصحيح، مراجعة الإجابات وتحليل المهارات.
3. **التوجيه بعد المحاولة:** تحديد مجالات الضعف، ترتيب الأولويات، وبناء برومبت مخصص لشرح الخطأ.
4. **المصادر:** كتب وروابط وشروحات وقنوات ومراجع مرتبة حسب المادة والوحدة.

لا توجد في النسخة الحالية حاجة إلى خادم تطبيق أو قاعدة بيانات أو تسجيل دخول؛ كل ما يحتاجه الطالب لتشغيل المنتج الأساسي يعمل من الملفات الساكنة وفي المتصفح.

## 2. مسار التشغيل الفعلي

```text
client/src/main.tsx
  → client/src/App.tsx
    → client/src/pages/Home.tsx
      → PromptLibrary
    → #exam-pilot (lazy)
      → ArabicExamTypography
      → ExamPilot
        → curriculumSkillIds
        → ExamRunner
          → ExamResultReport
            → LearningDashboard
```

هذا هو المسار الحي الذي يجب اعتباره مرجعًا عند تحديد ما إذا كان ملف واجهة أو اعتماد برمجي ما مستخدمًا فعليًا.

## 3. طبقات النظام

| الطبقة | الملفات/التقنية | المسؤولية |
|---|---|---|
| Product shell | `main.tsx`, `App.tsx`, `Home.tsx` | بدء التطبيق، التنقل بين التجربة الأساسية والمحاكاة، وعرض المسارات الرئيسية |
| Prompt engine | `PromptLibrary.tsx`, `data/promptCatalog.ts`, `shared/prompts/*` | اختيار الحالة وبناء البرومبت وسياسات الإجابة العربية |
| Curriculum domain | `shared/curriculum/curriculum-model.ts` | تعريف العقد الموحد للمادة والوحدة والدرس والمهارة وروابط المحتوى والتحقق من سلامة العلاقات |
| Curriculum registry | `data/curriculumStructure.ts`, `data/curriculum.ts` | بناء السجل الحي من المصادر الموجودة وربطه بالبرومبتات والأسئلة والمحاكاة |
| Curriculum/resources | `data/richCatalog.ts`, `data/unitExpansions.ts` | المصادر الخام والكتل الدراسية المنتقاة التي يغذي منها السجل الموحد |
| Exam taxonomy | `research/exams/calculus-skill-map.json` | المرجع الحالي لأسماء دروس ومهارات التفاضل والتكامل ومعرّفاتها وعلاقات المتطلبات السابقة |
| Exam data | `data/exams/*` | تعريف المحاكاة والأسئلة والمصدر وبيانات التحقق |
| Exam domain | `shared/exams/*` | نموذج السؤال والاختبار، الجلسة، التصحيح، التقرير، المعالجة والتحليل |
| Exam UI | `ExamPilot.tsx`, `ExamRunner.tsx`, `ExamResultReport.tsx`, `LearningDashboard.tsx` | تجربة الطالب داخل الاختبار وما بعده |
| Arabic math presentation | `ArabicExamTypography.tsx`, `ArabicRichContent.tsx`, `exams/arabic-math.ts`, `arabic-math.css` | عرض الرياضيات العربية والمحافظة على القراءة الصحيحة للأسئلة |
| Local persistence | `exams/session-storage.ts` + `localStorage` | حفظ محاولة الاختبار واستعادتها والتحقق من سلامة الجلسة |
| Delivery | Vite static build + Vercel + GitHub Pages | بناء نسخة ثابتة سريعة من المستودع نفسه |

## 4. نموذج المنهج الموحد

المرجع البرمجي للعلاقات التعليمية أصبح رسمًا مترابطًا بالترتيب:

```text
Subject
  → Unit
    → Lesson
      → Skill
        → inherited Sources
        → inherited Prompts
        → Questions that measure the skill
        → Simulations that contain those questions
```

العقد الأساسية معرفة في `shared/curriculum/curriculum-model.ts`، بينما يبني `client/src/data/curriculumStructure.ts` البنية من البيانات الحية الموجودة، ويضيف `client/src/data/curriculum.ts` روابط الأسئلة والمحاكاة المشتقة من تعريف الاختبار نفسه.

### قواعد الربط

- لكل مادة ووحدة ودرس ومهارة **معرّف ثابت**؛ العلاقات تحفظ بالمعرّفات لا بتطابق النصوص وقت التشغيل.
- معرّف المهارة الموجود أصلًا في `question.analysis.primarySkillId` و`secondarySkillIds` هو المفتاح نفسه في سجل المنهج؛ لا توجد قائمة مهارات ثانية خاصة بالاختبار.
- روابط الأسئلة والمحاكاة **مشتقة** من `pilotCalculusExam`، وليست نسخة يدوية أخرى قابلة للاختلاف عنه.
- المصادر والبرومبتات تورث باتجاه: المادة ← الوحدة ← الدرس ← المهارة. لذلك يمكن استرجاع سياق مهارة واحدة مع كل ما يساعد الطالب عليها من مكان واحد.
- أي سؤال يشير إلى مهارة غير موجودة في السجل يفشل في التحقق، كما أن المحاكاة نفسها لا تصبح جاهزة للطالب إذا خرجت مهارات أسئلتها عن السجل المعتمد.
- كل علاقة عكسية مهمة يتم فحصها أيضًا: المادة يجب أن تسجل وحداتها، والوحدة دروسها، والدرس مهاراته، والمحاكاة أسئلتها.

### مستوى الاكتمال الحالي

السجل يضم **كل المواد الحية** من `richCatalog.ts`، ويربطها بمصادرها والبرومبتات المنطبقة عليها. كما يضم كل الوحدات المنتقاة الموجودة حاليًا في `unitExpansions.ts`.

التفصيل إلى مستوى **الدرس والمهارة** مكتمل حاليًا لوحدة الرياضيات «التفاضل والتكامل»، اعتمادًا على الخريطة الموثقة `YEMEN-G12-MATH-CALCULUS-V1` الموجودة في `research/exams/calculus-skill-map.json`؛ ومنها 11 درسًا و39 مهارة.

بقية الوحدات الموجودة تسجل صراحة بحالة `unit-only` حتى يتوفر لها تفصيل موثق. لا تُنشأ أسماء دروس أو مهارات من التخمين لمجرد ملء النموذج.

هذه السياسة تجعل النموذج قابلًا للتوسع مادة بعد مادة دون تلويث المرجع المركزي بمحتوى غير مثبت.

## 5. عقد تنسيق الأسئلة — جزء محمي من المنتج

تنسيق الأسئلة الحالي ليس طبقة تجميل قابلة للحذف أثناء التنظيف. الملفات التالية تمثل **حدًا معماريًا محميًا**:

- `client/src/components/ArabicExamTypography.tsx`
- `client/src/components/ArabicRichContent.tsx`
- `client/src/exams/arabic-math.ts`
- `client/src/arabic-math.css`
- مكونات عرض السؤال والمراجعة داخل `ExamRunner.tsx` و`ExamResultReport.tsx`

أي إعادة هيكلة مستقبلية يجب أن تثبت، باختبار متصفح حقيقي، أن السؤال والخيارات والرموز العربية وMathML واتجاه RTL ما زالت معروضة بصورة صحيحة قبل الدمج.

## 6. نموذج الاختبار والتحقق

السؤال ليس نصًا حرًا فقط؛ نموذج `shared/exams/question-model.ts` يحتفظ بنوع السؤال والمحتوى الغني والخيارات والإجابة ودليلها والمصدر والمهارة والصعوبة والشرح وحالة التحقق.

لا يصبح السؤال جاهزًا للطالب إلا بعد اجتياز عقد التحقق. المحاكاة الحالية متكيفة بوضوح مع مراجع بنية الاختبارات ولا تُعرض كنص وزاري حرفي عندما لا تكون كذلك.

من هذه المرحلة أصبحت بوابة النشر تستقبل `curriculumSkillIds` من السجل الموحد؛ وبذلك لا يكفي أن يكون السؤال صحيح البنية، بل يجب أن تكون مهارته معروفة في المنهج المسجل أيضًا.

## 7. اختبارات اتساق المنهج

`client/src/data/curriculum.test.ts` يحمي النموذج من الانحراف عبر التحقق من:

- عدم وجود معرّفات مكررة أو مراجع يتيمة.
- تغطية كل المواد الحية والوحدات المنتقاة الحالية.
- بقاء خريطة التفاضل والتكامل 11 درسًا و39 مهارة كما هي في المرجع.
- ارتباط الأسئلة الخمسين بمهارات معروفة وبمحاكاة واحدة صحيحة.
- قدرة النظام على حل مسار كامل مثل `DER-CHAIN` إلى المادة والوحدة والدرس والمهارة والمصادر والبرومبتات والأسئلة والمحاكاة.
- عدم الادعاء بأن مهارة غير مقاسة حاليًا مثل `LIM-DIRECT` لها أسئلة أو محاكاة، مع بقائها مرتبطة بمصادر التعلم والبرومبتات.

تظل بوابة الإغلاق العامة: TypeScript + Vitest + production build + Browser Smoke للمسار الرئيسي و`#exam-pilot`.

## 8. التخزين والخصوصية

- لا توجد حسابات طلاب في النسخة الحالية.
- جلسة المحاكاة تحفظ في `localStorage` فقط.
- التخزين يتحقق من سلامة الجلسة ويتعامل مع القيم التالفة والتعارض بين نسخة أقدم وأحدث.
- لا يحتاج فتح الصفحة أو تشغيل المحاكاة إلى API داخلي أو خادم Express.

إذا ظهرت لاحقًا حاجة حقيقية للمزامنة بين الأجهزة أو لوحة إدارة، تضاف طبقة خادم مستقلة بقرار معماري صريح بدل إبقاء خادم غير مستخدم داخل البناء الحالي.

## 9. البناء والنشر

- **Build:** `pnpm build` → `vite build`.
- **Output:** `dist/public`.
- **Vercel:** يبني من جذر المستودع ويستخدم base `/`.
- **GitHub Pages:** يبني من المستودع نفسه، ومع `GITHUB_ACTIONS=true` يستخدم base `/yemen-grade12-learning-hub/`.
- المحاكاة وطبقة تنسيق الرياضيات تُحمّلان عند الطلب من `#exam-pilot` ولا تدخلان الحزمة الأولى للصفحة الرئيسية.

## 10. سياسة الاعتماديات

يُسمح بالاعتماد البرمجي عندما يخدم مسارًا حيًا في المنتج أو أداة بناء/اختبار مستخدمة فعلًا. لا تُحتفظ بمكتبات UI كاملة أو خادم أو أدوات تطوير خاصة بمنصة سابقة لمجرد أنها كانت جزءًا من قالب المشروع.

الاعتماديات الأساسية الحالية مقصودة لتكون صغيرة:

- Runtime: React, React DOM, Lucide.
- Build/style: Vite, React Vite plugin, Tailwind Vite plugin, Tailwind CSS.
- Quality: TypeScript, Vitest, Prettier وأنواع TypeScript اللازمة.

نموذج المنهج لا يضيف أي حزمة Runtime جديدة؛ هو مبني بالكامل بأنواع TypeScript وهياكل البيانات الحالية.

## 11. مصادر المحتوى

`richCatalog.ts` هو فهرس المصادر الحي، و`unitExpansions.ts` يضيف المسارات المنتقاة للوحدات. السجل الموحد لا ينسخ محتواهما، بل يحولهما إلى مراجع ثابتة قابلة للربط مع بقية المنهج. أداة `scripts/check-live-links.mjs` تظل مسؤولة عن فحص الروابط وكتابة التقرير إلى `reports/link-audit.json`.

أي مولّد محتوى يعتمد على مسارات محلية خارج المستودع لا يُعد جزءًا قابلًا لإعادة الإنتاج من المشروع، ولذلك لا يدخل في مسار البناء الرسمي.

## 12. قواعد التطوير من هذه النقطة

1. لا ننشئ نسخًا متتابعة مثل `HomeV2` و`HomeV3`؛ تُطوّر الصفحة الحية عبر Git وتاريخ الـcommits.
2. لا نضيف تبعية قبل وجود مستورد حي أو سبب بناء/اختبار واضح لها.
3. كل توسع في المنهج يمر عبر العقد: مادة → وحدة → درس → مهارة، مع حالة اكتمال صريحة بدل ملء الفجوات بالتخمين.
4. لا يوضع `primarySkillId` أو `secondarySkillId` في سؤال إلا إذا كان موجودًا في سجل المنهج.
5. روابط السؤال والمحاكاة تُشتق من بيانات الاختبار، ولا تحفظ كقائمة يدوية موازية.
6. منطق الاختبار يبقى في `shared/exams` قدر الإمكان، والواجهة تستهلكه بدل تكراره.
7. بيانات الأسئلة تبقى منفصلة عن مكونات العرض.
8. تنسيق الأسئلة والرياضيات العربية عقد محمي ويحتاج smoke test قبل الدمج.
9. أي تغيير في `package.json` يجب أن يحدث معه تحديث `pnpm-lock.yaml` وأن يمر `pnpm install --frozen-lockfile` بعد ذلك.
10. معيار إغلاق أي إعادة هيكلة: TypeScript + Vitest + production build + فحص متصفح للمسار الرئيسي و`#exam-pilot`.

## 13. ما ليس موجودًا عمدًا الآن

- Backend تطبيقي دائم.
- قاعدة بيانات.
- تسجيل دخول للطالب.
- مزامنة سحابية للمحاولات.
- CMS أو لوحة إدارة.
- تفاصيل دروس ومهارات لبقية الوحدات التي لم تُبنَ لها بعد خريطة موثقة داخل المستودع.

هذه ليست نواقص يتم إخفاؤها؛ هي حدود اكتمال صريحة. تضاف الطبقات والمحتوى فقط عندما تظهر متطلبات أو مراجع حقيقية تستدعيها.